image: node:20

pages:
  cache:
    paths:
      - frontend/node_modules/

  script:
    - cd frontend        # 👈 go into your vite app folder
    - npm ci
    - npm run build
    - mv dist ../public  # move build output for GitLab Pages

  artifacts:
    paths:
      - public

  only:
    - main

